# spec-authoring-contract - Test Plan

## 覆盖目标

1. 规范输入到 `spec_ref` 的主链路。
2. 缺关键输入时 Fail-Closed。
3. spec 结构不可解析时 Fail-Closed。

## 最小用例

1. TC-SAC-001: Happy path。
2. TC-SAC-002: objective 缺失。
3. TC-SAC-003: scope baseline 缺失。
